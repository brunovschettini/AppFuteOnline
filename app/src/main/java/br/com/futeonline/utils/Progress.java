package br.com.futeonline.utils;

import android.animation.Animator;
import android.animation.AnimatorListenerAdapter;
import android.animation.ObjectAnimator;
import android.annotation.TargetApi;
import android.os.Build;
import android.os.Bundle;
import android.os.Handler;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import android.view.animation.DecelerateInterpolator;
import android.widget.ProgressBar;


public class Progress extends AppCompatActivity {
/*
    private ProgressBar progressBar;
    private ObjectAnimator animation;

    private Handler mHandler = new Handler();

    public Progress() {
        progressBar = null;
        animation = null;
    }

    public Progress(Integer findViewById) {
        progressBar = (ProgressBar) this.findViewById(findViewById);
        // see this max value coming back here, we animale towards that value
        animation = ObjectAnimator.ofInt(progressBar, "progress", 0, 500);
        animation.setDuration(5000); //in milliseconds
        animation.setInterpolator(new DecelerateInterpolator());
    }

    public void start() {
        animation.start();
    }

    public void stop() {
        animation.end();
    }
    */
}
